import { styled } from "styled-components";

export const ContainerFeedbacks = styled.div`
 
  .starsWrapper {
    display: flex;
    gap: 5px;
  }

  .iconStar {
    width: 24px;
    height: 24px;
  }

 
`;
